# 历史资料归档

本目录只用于追溯，不作为当前需求、赛题规则或实现状态的权威来源。

- `旧赛事调研/`：早期赛事案例与赛制搜集，已由 `资料/01-赛事要求/` 的 2026 原题映射替代；
- `企划与交付参考/`、`前期方案/`：V1 前后备选方向和旧企划；
- `交流审查稿/`：用户保留的代码/产品审查和建议原稿；
- `对话快照/`：历史决策追溯，不作为当前指令；
- `发布产物/`：旧源码压缩包，二进制继续被 Git 忽略。

需要恢复旧结论时，先与 `doc/03-开发历史.md` 和当前代码交叉核验，不能直接复制回 `doc/02-项目规划.md`。
