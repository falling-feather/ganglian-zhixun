# 历史发布产物

这里保存从仓库根目录迁入的旧发布压缩包，便于人工追溯；ZIP 与校验文件继续由 Git 忽略，不属于当前源码或活跃资料。

- `V0.9.4/`：`ronggang-v0.9.4-source.zip` 及原 SHA-256 文件；未改写内容。
- 新发布产物仍由 `pnpm release:archive` 输出到根目录 `release-artifacts/`，验收后再按版本人工归档。
